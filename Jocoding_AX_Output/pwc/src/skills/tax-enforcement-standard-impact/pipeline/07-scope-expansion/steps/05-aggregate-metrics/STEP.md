# Step 5. Aggregate Metrics

- Compute recall and precision across all amendment events, not per single case.
- Report evidence rate, reason quality, hold quality.
- Emit a failure table keyed by Phase 6 failure labels.
- List any coverage cap (top-N, window bounds) explicitly.
